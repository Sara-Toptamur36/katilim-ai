# KatilimAI - Python bagimliliklari
# Sartname Md. 5.10 / 8: tumu acik kaynak, ucretli/kapali kaynak servis YOK.
# Lisanslar: FastAPI/Pydantic/SQLAlchemy/Alembic (MIT), uvicorn (BSD),
#            psycopg (LGPL - dinamik baglanti, kullanimda kisit yaratmaz),
#            python-jose (MIT), sse-starlette (BSD)
#            beautifulsoup4/html5lib (MIT), playwright (Apache-2.0),
#            pypdf/pandas/lxml (BSD)
#
# SURUM SABITLEME (==): Onceden hepsi ">=" (minimum surum) idi; bu, ayni
# commit'in farkli gunlerde farkli paket surumleriyle kurulmasina ve
# davranis degismesine yol aciyordu. Sartname Md. 6 "kodun calistirilmasi
# icin gerekli kurulum adimlari net belirtilmeli" diyor ve juri demosunun
# tekrar uretilebilir olmasi gerekiyor. Buradaki surumler, tam test
# suite'inin (299 test) gecerek dogrulandigi surumlerdir.
#
# Surum yukseltirken: once yerelde kurup TUM testleri calistirin, sonra
# buradaki pin'i guncelleyin - tersi degil.

# --- API katmani ---
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.4
python-multipart==0.0.28

# --- Kimlik dogrulama ---
# Not: passlib artik bakim gormuyor ve bcrypt>=4.1 ile uyumsuz (surum
# tespiti kirildi) - parola hashleme dogrudan bcrypt ile yapilir.
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# --- Veritabani ---
sqlalchemy==2.0.49
alembic==1.18.4
psycopg[binary]==3.3.4

# --- Streaming (Havin'in chatbot arayuzu icin) ---
sse-starlette==3.4.6

# --- Yerel LLM'e istek (Ollama) ---
requests==2.34.0

# --- Veri toplama / scraper (Zeynep) ---
# Not: requests yukarida zaten tanimli, tekrar eklenmedi.
beautifulsoup4==4.15.0
playwright==1.62.0
pypdf==6.14.2
pandas==2.3.3
lxml==6.1.1
html5lib==1.1

# --- Test ---
pytest==9.0.2
httpx==0.28.1

# --- NLP / Bilgi Cikarimi / RAG (NLP ekibi) ---
# Not: pydantic ve requests yukarida zaten tanimli, tekrar eklenmedi.
transformers==5.8.1
torch==2.12.0
sentence-transformers==5.5.0
qdrant-client==1.18.0
langchain-text-splitters==1.1.2
gliner==0.2.28
tiktoken==0.13.0

# --- Altin Veri Seti (Excel okuma) ---
openpyxl==3.1.5
